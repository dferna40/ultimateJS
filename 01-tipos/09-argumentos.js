//Definición de funcion con parametros
function suma(numero1,numero2) {
    return numero1 +numero2;
}

// Llamada a la función suma con el argumento informado y asignación a variable
let resultado = suma(8,2);

console.log(suma(8,2));

// Imprimir el tipo
console.log (typeof suma);